<h1 class="text-center mt-4">Log in</h1> <!-- mt-4 stands for margin-top of 4 (idk what units)-->

<div class="container">

  <div class="row justify-content-center">
    <div class="col-10">
      <%= form_with(scope: :session, url: login_path, class: "shadow p-3 mb-3 bg-gradient rounded", id: "new-form-color", local: true) do |f| %> <!-- Since we're not dealing with a model, we change to scope, refer to users/_form.html.erb to compare. Also added url: login_path, refer to post on routes.rb -->
        <div class="form-group row mt-3">
          <%= f.label :email, class: "col-2 col-form-label text-light label-responsive" %>
          <div class="col-10 form-responsive">
            <%= f.email_field :email, rows: 13, class: "form-control shadow rounded", placeholder: "Enter your email" %> <!-- Take up 13 rows -->
          </div>
        </div>

        <div class="form-group row mt-3">
          <%= f.label :password, class: "col-2 col-form-label text-light label-responsive" %>
          <div class="col-10 form-responsive">
            <%= f.password_field :password, rows: 13, class: "form-control shadow rounded", placeholder: "Enter your password" %> <!-- Take up 13 rows -->
          </div>
        </div>

        <div class="form-group row justify-content-center mt-3 mb-4 ">
          <%= f.submit "Log in", class: "btn btn-outline-light shadow-none btn-lg col-md-auto" %> <!-- IMPORTANT, no comma needed between submit and class! BUT since we've added what it needs to say "Sign up" we DO need a comma now, refer to other form partial for articles to see example without comma necessity. Additional Edit: We used a ternary operator to decide whether the button should say Sign up or Update account based on if the user is new or not -->
        </div>
      <% end %>
    </div>

    <div class="col-md-3 text-center mb-3">
      <%= link_to '[ Cancel and return to articles listing ]', articles_path, id: "new-form-color-cancel" %>
    </div>
  </div>

</div>